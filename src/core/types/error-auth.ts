export enum ErrorAuth {
	ACCOUNT_BLOCKED = 'block',
	ACCOUNT_EXISTS = 'account_already_exists',
	AUTH = 'auth',
	TWITCH = 'auth',
	YOUTUBE = 'youtube-auth',
	TROVO = 'trovo-auth',
	VK_PLAY = 'vkplay-auth',
	TIKTOK = 'tiktok-auth',
}

export enum ErrorReason {
	USER = 'user',
}
